# Build artifacts
obj_*/
*.o
*.a
*.so
*.mod
*.smod

# Fortran fpm build directory
build/

# Generated by make (per-compiler options and dependency files)
foptions_*.mk
depends.mk

# SVN working copy metadata (git ignores .svn natively, but be explicit)
.svn/

# Local scratch / commit notes
commit-msg

# Parity harness build and run artefacts
tools/parity/bin/
tools/parity/.work/
